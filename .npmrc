# ~/.npmrc - npm configuration
# Configuration for Node.js package manager (npm)

# =============================================================================
# REGISTRY SETTINGS
# =============================================================================

# Default registry (official npm registry)
registry=https://registry.npmjs.org/

# Registry for scoped packages (example for organization)
# @myorg:registry=https://npm.myorg.com/

# =============================================================================
# SECURITY SETTINGS
# =============================================================================

# Audit level for security vulnerabilities
audit-level=moderate

# Automatically fix vulnerabilities when possible
# fund=false

# =============================================================================
# INSTALLATION SETTINGS
# =============================================================================

# Save installed packages to package.json by default
save=true

# Use exact versions instead of ranges
save-exact=true

# Install packages as devDependencies by default with --save-dev
# save-dev=true

# Package lock settings
package-lock=true

# Shrinkwrap settings
shrinkwrap=true

# =============================================================================
# CACHE SETTINGS
# =============================================================================

# Cache directory (default: ~/.npm)
# cache=~/.npm

# Cache verification
cache-lock-retries=10
cache-lock-stale=60000
cache-lock-wait=10000

# Cache maximum age in seconds (default: 86400 = 1 day)
cache-max=86400

# Minimum cache TTL for HTTP cache
cache-min=10

# =============================================================================
# PROGRESS AND OUTPUT
# =============================================================================

# Progress bar display
progress=true

# Logging level (silent, error, warn, notice, http, timing, info, verbose, silly)
loglevel=notice

# Use unicode characters in output
unicode=true

# Color output
color=true

# =============================================================================
# DEVELOPMENT SETTINGS
# =============================================================================

# Engine strict mode (fail if Node.js version doesn't match)
engine-strict=false

# Optional dependencies
optional=true

# Development dependencies in production
production=false

# =============================================================================
# NETWORK SETTINGS
# =============================================================================

# Network timeout settings
fetch-retries=2
fetch-retry-factor=10
fetch-retry-mintimeout=10000
fetch-retry-maxtimeout=60000

# Network timeout
fetch-timeout=60000

# HTTPS proxy
# https-proxy=http://proxy.company.com:8080

# HTTP proxy
# proxy=http://proxy.company.com:8080

# No proxy for these domains
# noproxy=localhost,127.0.0.1,.local

# =============================================================================
# GLOBAL PACKAGE SETTINGS
# =============================================================================

# Global installation prefix
# prefix=~/.npm-global

# Global packages directory
# globalconfig=~/.npmrc
# globalignorefile=~/.npmignore

# =============================================================================
# SCRIPTS AND LIFECYCLE
# =============================================================================

# Scripts shell (default: /bin/sh on POSIX, cmd on Windows)
# script-shell=/bin/bash

# Ignore scripts during install
ignore-scripts=false

# =============================================================================
# VERSION AND TAGGING
# =============================================================================

# Default tag for published packages
tag=latest

# Version commit message
message="Release version %s"

# Tag version prefix
tag-version-prefix=v

# =============================================================================
# PUBLISHING SETTINGS
# =============================================================================

# Access level for published packages (public, restricted)
access=public

# Allow publishing
# publish-config={"tag": "beta"}

# =============================================================================
# NODE.JS SETTINGS
# =============================================================================

# Node.js options
# node-options=--max-old-space-size=4096

# Target platform and architecture
# target_platform=linux
# target_arch=x64

# =============================================================================
# WORKSPACE SETTINGS (for npm 7+)
# =============================================================================

# Include workspace root
include-workspace-root=false

# Workspace dependencies
workspaces-update=true

# =============================================================================
# MISCELLANEOUS
# =============================================================================

# Update notifier
update-notifier=true

# Fund messages
fund=true

# Init settings
init-author-name="Your Name"
init-author-email="your.email@example.com"
init-author-url="https://yourwebsite.com"
init-license="MIT"
init-version="1.0.0"

# =============================================================================
# NOTES
# =============================================================================

# This configuration provides:
# 1. Secure default settings
# 2. Reasonable network timeouts and retries
# 3. Progress indication and colored output
# 4. Sensible caching configuration
# 5. Development-friendly defaults
# 6. Security audit configuration

# To override settings for specific projects:
# 1. Create a .npmrc file in your project directory
# 2. Use command-line flags: npm install --save-exact
# 3. Use environment variables: NPM_CONFIG_REGISTRY=...

# Common npm commands with these settings:
# npm install                    # Install dependencies
# npm audit                      # Check for vulnerabilities
# npm audit fix                  # Fix vulnerabilities automatically
# npm update                     # Update dependencies
# npm outdated                   # Check for outdated packages
# npm list                       # List installed packages
# npm cache clean --force        # Clear npm cache

# For private registries:
# npm config set registry https://npm.mycompany.com
# npm config set //npm.mycompany.com/:_authToken TOKEN

# For scoped packages:
# npm config set @myorg:registry https://npm.mycompany.com
